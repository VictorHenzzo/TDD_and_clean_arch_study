name: tdd_study
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get_it:
  flutter_bloc:
  equatable:
  dartz:
  connectivity:
  dio:
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail:

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
